export async function triggerPulseAudio(enabled: boolean): Promise<void> {
  if (!enabled) return;
}

export async function triggerStabilizeAudio(enabled: boolean): Promise<void> {
  if (!enabled) return;
}

export async function triggerCollapseWarningAudio(
  enabled: boolean,
): Promise<void> {
  if (!enabled) return;
}
